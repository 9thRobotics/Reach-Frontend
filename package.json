{
  "name": "reach-frontend",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://9thRobotics.github.io/Reach-Frontend",
  "dependencies": {
    "ethers": "^6.7.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.0",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.3"
  },
  "scripts": {
    "dev": "vite",                 // Start local development server
    "build": "vite build",         // Build for production
    "preview": "vite preview",     // Preview production build
    "predeploy": "npm run build",  // Build before deploying
    "deploy": "gh-pages -d build"  // Deploy the build folder
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}

